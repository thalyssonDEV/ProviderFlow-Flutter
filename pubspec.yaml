name: maps_disp_moveis_project
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path: ^1.8.3
  crypto: ^3.0.3
  brasil_fields: ^1.15.0
  google_maps_flutter: ^2.5.3
  geolocator: ^13.0.0
  url_launcher: ^6.3.0
  shared_preferences: ^2.3.2
  geocoding: ^3.0.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon.png"
  min_sdk_android: 21

flutter:
  uses-material-design: true